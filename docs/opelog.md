# Vdasync operations logs

Operations logs record events on files during their processing

- to enable efficient operations at scale
- to provide reports for traceability

The `vdasync` CLI leverages operations logs on option to enable those features.

Documentation to be written